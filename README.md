# Download Template
```bash
git clone https://github.com/JeffWeim/parcel-static-site.git
 ```
 
 ```bash
cd parcel-static-site
 ```
 
 ```bash
rm -rf .git && git init
 ```

# Install

`yarn`

# Dev

`yarn start`
